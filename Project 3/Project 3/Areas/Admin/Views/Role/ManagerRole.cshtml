@using PagedList.Mvc;
@model PagedList.IPagedList<Project_3.Models.REL_MANAGER_ROLES>
@{
    ViewBag.Title = "Manager Role";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<div class="content-wrapper">
    <div class="row">
        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h1 class="card-title">Role - @ViewBag.RoleName</h1>
                    @using (Html.BeginForm("ManagerRole", "Role", FormMethod.Get))
                    {
                        <div class="form-group">
                            <input type="text" class="form-control" name="search" id="search" placeholder="Search Username or Full Name" style="width:30%">
                            <button type="submit" class="btn btn-primary">Search</button>
                        </div>
                    }
                    @Html.ActionLink("Add", "AddManagerToRole", "Role", new { area = "Admin", id = @ViewBag.RoleId }, new { @class = "btn btn-inverse-primary btn-fw" })
                    @Html.ActionLink("Back", "", "Role", new { area = "Admin" }, new { @class = "btn btn-outline-secondary btn-fw" })
                    <div class="table-responsive pt-3">
                        <table class="table table-bordered">
                            <thead>
                                <tr>
                                    <th>#</th>
                                    <th>Full Name</th>
                                    <th>Date of Birth</th>
                                    <th>Gender</th>
                                    <th>Phone Number</th>
                                    <th>Email</th>
                                    <th>Address</th>
                                    <th>Username</th>
                                    <th>Action</th>
                                </tr>
                            </thead>
                            <tbody>
                                @if (Model.Any())
                                {
                                    int i = (ViewBag.Page * ViewBag.PageSize) - (ViewBag.PageSize - 1);
                                    foreach (var item in Model)
                                    {
                                        <tr>
                                            <td>@(i++)</td>
                                            <td>@item.MANAGER.FullName</td>
                                            <td>@item.MANAGER.Birth.Day/@item.MANAGER.Birth.Month/@item.MANAGER.Birth.Year</td>
                                            <td>@item.MANAGER.Gender</td>
                                            <td>@item.MANAGER.PhoneNumber </td>
                                            <td>@item.MANAGER.Email</td>
                                            <td>@item.MANAGER.Address</td>
                                            <td>@item.MANAGER.Username</td>
                                            <td>
                                                @using (Html.BeginForm("ManagerRole", "Role", FormMethod.Post))
                                                {
                                                    <input name="ManagerId" value=@item.ManagerId hidden />
                                                    <button type="submit" class="btn btn-link" title="Remove" style="padding:0;"><i class="mdi mdi-minus-circle-outline" style="font-size:25px;color:red"></i></button>
                                                }
                                            </td>
                                        </tr>
                                    }
                                }
                                else
                                {
                                    <tr>
                                        <td colspan="9" style="text-align:center; font-size:large;">
                                            <b>No Data</b>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                    <div class="center">
                        @Html.PagedListPager(Model, p => Url.Action("ManagerRole", new { page = p, searchPage = ViewBag.Search }))
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@{
    Project_3.Data.Common.Page = ViewBag.Page;
    Project_3.Data.Common.Search = ViewBag.Search;
}